<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Genetic Item</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.3/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

    <div class="container mx-auto p-6">
        <h2 class="text-2xl font-semibold mb-4 text-green-500">Add New Genetic Item</h2>

        <!-- Form to add a new genetic item -->
        <form action="/inventory/genetic-inventory" method="POST" class="bg-white p-6 rounded-lg shadow-md">

            <!-- Type Selection -->
            <div class="mb-4">
                <label for="type" class="block text-gray-700 font-semibold">Type</label>
                <select name="type" id="type" class="w-full p-2 border border-gray-300 rounded-lg" required>
                    <option value="spore print">Spore Print</option>
                    <option value="spore syringe">Spore Syringe</option>
                    <option value="liquid culture">Liquid Culture</option>
                    <option value="agar plate">Agar Plate</option>
                    <option value="agar slant">Agar Slant</option>
                </select>
            </div>

            <!-- Species Input -->
            <div class="mb-4">
                <label for="species" class="block text-gray-700 font-semibold">Species</label>
                <input type="text" name="species" id="species" class="w-full p-2 border border-gray-300 rounded-lg" required>
            </div>

            <!-- Strain Input -->
            <div class="mb-4">
                <label for="strain" class="block text-gray-700 font-semibold">Strain</label>
                <input type="text" name="strain" id="strain" class="w-full p-2 border border-gray-300 rounded-lg" required>
            </div>

             <!-- isolation Input -->
             <div class="mb-4">
                <label for="isolation" class="block text-gray-700 font-semibold">Isolation (optional)</label>
                <input value="N/A" type="text" name="isolation" id="iosaltion" class="w-full p-2 border border-gray-300 rounded-lg" >
            </div>

            

            <!-- Quantity Input -->
            <div class="mb-4">
                <label for="quantity" class="block text-gray-700 font-semibold">Quantity</label>
                <input type="number" name="quantity" id="quantity" class="w-full p-2 border border-gray-300 rounded-lg" min="1" required>
            </div>

            <!-- Submit Button -->
            <div class="flex justify-end">
                <button type="submit" class="bg-green-500 text-white px-6 py-2 rounded-lg hover:bg-green-600">Add Genetic Item</button>
            </div>

        </form>
    </div>

</body>
</html>
